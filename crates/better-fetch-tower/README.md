# better-fetch-tower

Companion crate for [better-fetch](https://crates.io/crates/better-fetch) Tower transport integration.

Enable the `tower` feature on `better-fetch`, or depend on this crate for an explicit transport-extension dependency:

```toml
better-fetch-tower = "0.1"
```

```rust
use better_fetch_tower::*;
```
